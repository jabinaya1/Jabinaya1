def linearSearchProduct(productList,
targetProduct);
 indices=【】

 for index,Product in
enumerate(productList):
  if product==targetproduct:
   indices.append(index)

  return indices 

#Example usage:
product=["shoes","boot","loafer",
"shoes","sandal","shoes"]
target="shoes"
target2='apple'
result=linearSearchProduct(Products,
target)
print(result)

